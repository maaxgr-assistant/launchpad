# Launchpad Dashboard

## Nach jeder Änderung

Nach jeder inhaltlichen Änderung am Projekt immer folgende Schritte ausführen:

1. Änderungen committen und zu Git pushen (`git add`, `git commit`, `git push`)
2. Container neu starten: `docker compose down && docker compose up -d`
